asgiref==3.11.0
Django==6.0.8
django-ckeditor-5==0.2.20
pillow==12.3.0
sqlparse==0.6.0
uvicorn[standard]==0.34.0
psycopg2-binary==2.9.10
django-storages[google]==1.14.4
google-cloud-secret-manager==2.21.0
whitenoise==6.8.2
channels==4.2.0
channels-redis==4.2.1
# Server-side HTML sanitizer for CKEditor content (S6-01) — the actual
# stored-XSS boundary; the editor's htmlSupport config is client-side only.
nh3==0.3.7
# Content-Security-Policy header middleware (S6-10 / S7-03).
django-csp==4.0
# Failed-login throttling/lockout on the public auth surface (S1-05).
django-axes==8.3.1
# Both arrive transitively via google-cloud-secret-manager, but
# demo/agent_client.py imports them directly to mint Cloud Run ID tokens.
# Pin what you import.
google-auth==2.56.2
requests==2.34.2