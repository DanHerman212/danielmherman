# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.venv/

# Django
db.sqlite3
local_settings.py
staticfiles/
media/

# Node dependencies — installed by `npm ci`, never committed.
node_modules/

# Built console JavaScript. Output, not source — `npm run build` produces it
# locally (scripts/dev.sh runs it before starting the server) and the
# Dockerfile's jsbuild stage produces it for production. Never committed: a
# stored copy can go stale silently.
static/js/bundled/

# macOS
.DS_Store

# Demo: real captured note passages (MIMIC-derived) — must never ship
demo/data/demo_fixtures/rag_*.json

# Dev spikes (A2UI proof-of-concept)
static/spikes/
