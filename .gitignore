# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.venv/

# Django
db.sqlite3
local_settings.py
staticfiles/
media/

# Build diagnostics (scripts/bundle_console_js.sh records the module graph here)
a2ui_bundle.meta.json

# Built console JavaScript. Output, not source — `scripts/dev.sh` produces it
# before starting the local server, and the Dockerfile's jsbuild stage produces
# it for production. Never committed: a stored copy can go stale silently.
static/js/bundled/

# macOS
.DS_Store

# Demo: real captured note passages (MIMIC-derived) — must never ship
demo/data/demo_fixtures/rag_*.json

# Dev spikes (A2UI proof-of-concept)
static/spikes/
