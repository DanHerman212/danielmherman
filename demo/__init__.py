"""Demo app: the clinical copilot's patient cohort and per-user quota.

Kept separate from `content` (the portfolio itself) because this is demo
infrastructure with a different lifecycle — it is reseeded whenever the model
is retrained.
"""
